# @windtribe/database

Supabase schema definitions, SQL migrations, and generated TypeScript types. Consumed by every app that talks to the backend.

Placeholder — migrations and generated types land once Supabase is wired up in Epic 1.
